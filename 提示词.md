# 视频文件管理系统（Video File Manager）软件开发提示词文档

## 一、项目概述

### 1.1 项目背景与目标

随着本地视频素材、影视资源的积累，传统的文件资源管理器难以满足对视频元数据（如分辨率、帧率、编码格式）的直观查看及观看行为分析的需求。本项目旨在开发一款轻量级、跨平台的桌面端视频文件管理系统，通过自动化扫描、元数据提取、观看日志记录与可视化统计，帮助用户高效管理视频资产并量化个人观看习惯。

### 1.2 目标用户画像

- 视频创作者/剪辑师：需要快速检索素材、查看视频技术参数、管理项目文件夹。

- 影视爱好者/收藏家：拥有大量本地视频，希望记录观看进度、统计观看频次，并进行分类标签管理。

- 内容审核/研究人员：需要对特定目录下的视频进行批量管理、元数据导出与行为分析。

### 1.3 核心价值主张

- 极致轻量：基于 Tauri 2.x，安装包约 5MB，内存占用极低。

- 数据掌控：所有元数据与日志本地化存储（SQLite），无需联网，保护隐私。

- 智能洞察：自动生成观看趋势、高频视频排行榜，将枯燥的文件列表转化为可视化数据。

- 灵活组织：支持多维度标签体系，突破物理文件夹层级的限制。

## 二、功能需求规格

### 2.1 文件扫描与树状结构模块

| 功能名称      | 功能描述                           | 用户故事                               | 验收标准                                                                     | 优先级 |
| --------- | ------------------------------ | ---------------------------------- | ------------------------------------------------------------------------ | --- |
| 根文件夹选择与扫描 | 用户可选择本地任意文件夹作为视频库根目录，系统触发全量扫描。 | 作为用户，我希望指定一个包含所有视频的文件夹，让系统自动识别。    | 1. 支持系统原生文件夹选择对话框。2. 选择后自动触发后台扫描任务。                                      | P0  |
| 递归扫描与增量更新 | 深度遍历子文件夹，仅对新增或修改时间变化的文件提取元数据。  | 作为用户，我希望添加新视频后，系统能快速识别，而不是每次都全量扫描。 | 1. 支持递归遍历。2. 基于文件修改时间（mtime）进行增量扫描。3. 扫描过程不阻塞UI。                         | P0  |
| 视频格式识别    | 过滤非视频文件，仅处理指定后缀名的媒体文件。         | 作为用户，我只希望看到视频文件，忽略字幕、图片等无关文件。      | 支持：mp4, mkv, avi, mov, webm, flv, wmv, m4v, 3gp。                         | P0  |
| 树状目录组件    | 以树形结构展示文件夹层级，支持展开、折叠、搜索与过滤。    | 作为用户，我希望像资源管理器一样导航我的视频库。           | 1. 异步加载子节点（懒加载）。2. 支持按名称模糊搜索。3. 支持按标签/类型过滤。                              | P0  |
| 元信息提取     | 获取视频的分辨率、时长、编码、帧率等，并生成封面截图。    | 作为用户，我不打开视频就能知道它的画质和格式。            | 1. 使用 ffmpeg 或 mediainfo 提取信息。2. 使用 image crate 处理截图。3. 提取失败时记录错误但不中断扫描。 | P0  |
| 扫描进度反馈    | 实时显示当前扫描路径、已扫描文件数、进度百分比。       | 作为用户，扫描大文件夹时，我希望知道进度而不是以为程序卡死。     | 1. 提供进度条组件。2. 支持取消扫描操作。                                                  | P1  |

### 2.2 视频播放与打开日志模块

| 功能名称    | 功能描述                             | 用户故事                      | 验收标准                                                              | 优先级 |
| ------- | -------------------------------- | ------------------------- | ----------------------------------------------------------------- | --- |
| 视频播放集成  | 点击视频文件调用系统默认播放器或在应用内嵌播放器打开。      | 作为用户，我希望在系统内直接预览视频。       | 1. 支持配置外部播放器路径。2. 支持 Tauri 原生窗口播放（可选）。                            | P0  |
| 打开次数统计  | 每次触发播放操作，对应视频的 open_count 自动 +1。 | 作为用户，我希望系统自动记录我看了多少次这个视频。 | 1. 触发播放即计数。2. 数据库原子更新，防并发错误。                                      | P0  |
| 打开时间日志  | 记录精确到秒的打开时间、关闭时间及计算出的观看时长。       | 作为用户，我想回顾昨天看了哪些视频，看了多久。   | 1. 记录 open_time, close_time, duration。2. 异常关闭时（如程序崩溃），下次启动时补全或标记。 | P0  |
| 日志筛选与导出 | 支持按日期范围、视频ID筛选日志，并导出为 CSV。       | 作为用户，我想把观看记录导出来做进一步分析。    | 1. 支持日期范围选择器。2. CSV 包含：视频名、路径、打开时间、时长。                            | P1  |

### 2.3 统计图表与排行榜模块

| 功能名称   | 功能描述               | 用户故事                   | 验收标准                                       | 优先级 |
| ------ | ------------------ | ---------------------- | ------------------------------------------ | --- |
| 仪表盘总览  | 首页展示核心指标卡片。        | 作为用户，我打开软件就能看到库的整体规模。  | 展示：总视频数、总文件夹数、总观看次数、总观看时长。                 | P0  |
| 观看趋势图  | 折线图展示日/周/月观看时长与频次。 | 作为用户，我想看最近一周是不是看视频太多了。 | 1. 支持日/周/月切换。2. 双Y轴（时长+次数）。                | P1  |
| 排行榜    | 列表展示 TOP10 数据。     | 作为用户，我想找出我最爱的视频。       | 1. 最多打开 TOP10。2. 最长观看 TOP10。3. 最近活跃 TOP10。 | P1  |
| 标签统计   | 饼图/柱状图展示各标签下的视频分布。 | 作为用户，我想看我的素材库哪类内容最多。   | 1. 按标签聚合视频数。2. 支持点击扇区筛选列表。                 | P2  |
| 时间维度分析 | 热力图展示一周内各时间段的观看习惯。 | 作为用户，我想知道自己通常在几点看视频。   | 7x24 热力图矩阵。                                | P2  |

### 2.4 标签管理系统模块

| 功能名称            | 功能描述               | 用户故事                         | 验收标准                           | 优先级 |
| --------------- | ------------------ | ---------------------------- | ------------------------------ | --- |
| 标签 CRUD         | 创建、编辑、删除自定义标签。     | 作为用户，我想建立自己的分类体系。            | 1. 标签名唯一。2. 支持颜色/图标属性。         | P0  |
| 视频多标签关联         | 单个视频可打多个标签。        | 作为用户，一个视频可能既是“教程”又是“Python”。 | 1. 支持批量打标签。2. 支持从右键菜单快速打标。     | P0  |
| 标签分类（Tag Group） | 将标签归类（如：类型、心情、项目）。 | 作为用户，标签多了之后我需要分组管理。          | 1. 支持创建标签组。2. 标签必须属于某个组（或默认组）。 | P1  |
| 按标签筛选           | 在列表页通过标签组合过滤视频。    | 作为用户，我想快速找出所有“未看”且“高分”的视频。   | 1. 支持 AND/OR 逻辑组合。2. 筛选结果实时更新。 | P0  |

### 2.5 文件管理模块

| 功能名称  | 功能描述                          | 用户故事                  | 验收标准                              | 优先级 |
| ----- | ----------------------------- | --------------------- | --------------------------------- | --- |
| 元信息编辑 | 修改系统内的视频备注、自定义标题（不修改物理文件）。    | 作为用户，我想给视频加个备注方便搜索。   | 1. 仅修改数据库记录。2. 支持批量编辑。            | P1  |
| 批量操作  | 批量打标签、批量移动（逻辑移动/物理移动）、批量删除记录。 | 作为用户，整理几百个视频时不能一个个点。  | 1. 支持 Shift/Ctrl 多选。2. 物理移动需二次确认。 | P1  |
| 全局搜索  | 按文件名、路径、标签、备注搜索。              | 作为用户，我想通过关键词瞬间定位视频。   | 1. 支持模糊匹配。2. 搜索结果高亮关键词。           | P0  |
| 多视图切换 | 列表、网格、详细信息视图。                 | 作为用户，找封面时用网格，看参数时用列表。 | 1. 状态持久化。2. 网格视图支持懒加载图片。          | P1  |

## 三、补充功能建议

基于对视频管理场景的理解，建议补充以下增强体验的功能：

- 视频封面/缩略图预览：利用 ffmpeg 在扫描时截取第 10% 处的帧作为封面，存入本地缓存目录，网格视图下展示，极大提升浏览效率。
- 播放历史记录时间轴：在视频详情页展示该视频的历史播放时间轴（Gantt Chart 风格），直观看到“什么时候看的”、“看了哪一段”。
- 数据备份与恢复：提供“导出数据库”和“导入数据库”功能，支持将 SQLite 文件及封面缓存打包为 .vfm-backup，防止误删。
- 暗色/亮色主题切换：视频管理软件通常在暗光环境使用，暗色模式为刚需。
- 键盘快捷键支持：Ctrl+F 搜索、Enter 播放、T 打标签、↑/↓ 切换选中，提升操作效率。
- 多语言支持：基于 react-i18next 实现中英文切换，便于国际化。
- 数据导入：支持从 CSV/Excel 导入历史观看记录，方便从其他播放器迁移数据。
- 视频文件夹监控：使用 Rust notify crate 监听根目录文件变化（创建/删除/修改），自动触发增量扫描，无需手动刷新。
- 视频去重检测：基于 文件大小 + MD5/SHA256 检测重复视频，提供合并或删除建议。
- 用户偏好设置：默认扫描路径、自动扫描间隔、外部播放器路径、缩略图质量、忽略文件夹（如 .git, node_modules）等。

## 四、数据库设计

### 4.1 ER 关系描述

- folders 1:N videos（一个文件夹下多个视频）

- folders 自关联（父文件夹）

- videos M:N tags（通过 video_tags 关联）

- tags N:1 tag_groups（标签属于某个分类）

- videos 1:N open_logs（一个视频多条打开记录）

- settings 单表键值对存储
  
  ### 4.2 DDL (SQLite)
  
  ```sql
  -- 文件夹表CREATE TABLE IF NOT EXISTS folders (    id INTEGER PRIMARY KEY AUTOINCREMENT,    parent_id INTEGER,    name TEXT NOT NULL,    path TEXT NOT NULL UNIQUE,    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,    FOREIGN KEY (parent_id) REFERENCES folders(id) ON DELETE CASCADE);-- 视频表CREATE TABLE IF NOT EXISTS videos (    id INTEGER PRIMARY KEY AUTOINCREMENT,    folder_id INTEGER NOT NULL,    file_name TEXT NOT NULL,    file_path TEXT NOT NULL UNIQUE,    file_size INTEGER NOT NULL,          -- 字节    duration REAL,                       -- 秒    width INTEGER,                       -- 分辨率宽    height INTEGER,                      -- 分辨率高    codec TEXT,                          -- 编码格式    fps REAL,                            -- 帧率    sample_rate INTEGER,                 -- 采样率    cover_path TEXT,                     -- 缩略图本地路径    custom_title TEXT,                   -- 自定义标题    notes TEXT,                          -- 备注    open_count INTEGER DEFAULT 0,        -- 打开次数    file_hash TEXT,                      -- 文件哈希（去重用）    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,    modified_at DATETIME DEFAULT CURRENT_TIMESTAMP, -- 文件修改时间    scanned_at DATETIME DEFAULT CURRENT_TIMESTAMP,  -- 扫描入库时间    FOREIGN KEY (folder_id) REFERENCES folders(id) ON DELETE CASCADE);-- 标签组表CREATE TABLE IF NOT EXISTS tag_groups (    id INTEGER PRIMARY KEY AUTOINCREMENT,    name TEXT NOT NULL UNIQUE,           -- 如：类型、心情    sort_order INTEGER DEFAULT 0);-- 标签表CREATE TABLE IF NOT EXISTS tags (    id INTEGER PRIMARY KEY AUTOINCREMENT,    group_id INTEGER,    name TEXT NOT NULL,    color TEXT DEFAULT '#1890ff',        -- 标签颜色    FOREIGN KEY (group_id) REFERENCES tag_groups(id) ON DELETE SET NULL);-- 视频-标签关联表CREATE TABLE IF NOT EXISTS video_tags (    video_id INTEGER NOT NULL,    tag_id INTEGER NOT NULL,    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,    PRIMARY KEY (video_id, tag_id),    FOREIGN KEY (video_id) REFERENCES videos(id) ON DELETE CASCADE,    FOREIGN KEY (tag_id) REFERENCES tags(id) ON DELETE CASCADE);-- 打开日志表CREATE TABLE IF NOT EXISTS open_logs (    id INTEGER PRIMARY KEY AUTOINCREMENT,    video_id INTEGER NOT NULL,    open_time DATETIME NOT NULL,    close_time DATETIME,    duration REAL,                       -- 实际观看秒数    status TEXT DEFAULT 'active',        -- active, closed, crashed    FOREIGN KEY (video_id) REFERENCES videos(id) ON DELETE CASCADE);-- 系统设置表CREATE TABLE IF NOT EXISTS settings (    key TEXT PRIMARY KEY,    value TEXT NOT NULL,    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP);
  ```

```
### 4.3 索引设计
```sql
-- 优化树状结构查询CREATE INDEX idx_folders_parent ON folders(parent_id);-- 优化路径查找CREATE INDEX idx_videos_path ON videos(file_path);-- 优化文件夹下视频列表CREATE INDEX idx_videos_folder ON videos(folder_id);-- 优化打开日志查询CREATE INDEX idx_open_logs_video ON open_logs(video_id);CREATE INDEX idx_open_logs_time ON open_logs(open_time);-- 优化标签筛选CREATE INDEX idx_video_tags_tag ON video_tags(tag_id);-- 优化去重查询CREATE INDEX idx_videos_hash ON videos(file_hash);
```

## 五、后端 API 设计（Tauri Commands）

所有命令均使用 #[tauri::command] 宏，异步操作返回 Result<T, String>。

### 5.1 文件扫描模块

```rust
// 选择根文件夹并启动扫描#[tauri::command]async fn scan_root_folder(path: String) -> Result<ScanProgress, String>;// 获取扫描进度#[tauri::command]async fn get_scan_progress() -> Result<ScanProgress, String>;// 取消扫描#[tauri::command]async fn cancel_scan() -> Result<(), String>;// 获取文件夹树（懒加载，仅返回子节点）#[tauri::command]async fn get_folder_children(parent_id: Option<i64>) -> Result<Vec<FolderNode>, String>;
```

### 5.2 视频管理模块

```rust
// 获取视频列表（支持分页、筛选、排序）#[tauri::command]async fn list_videos(query: VideoQuery) -> Result<PageResult<VideoInfo>, String>;// 获取视频详情#[tauri::command]async fn get_video_detail(video_id: i64) -> Result<VideoDetail, String>;// 更新视频元信息（备注、自定义标题）#[tauri::command]async fn update_video_meta(video_id: i64, meta: VideoMetaUpdate) -> Result<(), String>;// 批量删除视频记录#[tauri::command]async fn batch_delete_videos(video_ids: Vec<i64>) -> Result<u64, String>;// 搜索视频#[tauri::command]async fn search_videos(keyword: String, limit: u32) -> Result<Vec<VideoInfo>, String>;
```

### 5.3 标签管理模块

```rust
// 获取所有标签组及标签#[tauri::command]async fn get_tag_tree() -> Result<Vec<TagGroupWithTags>, String>;// 创建/更新/删除标签#[tauri::command]async fn upsert_tag(tag: TagInput) -> Result<Tag, String>;#[tauri::command]async fn delete_tag(tag_id: i64) -> Result<(), String>;// 为视频设置标签（全量覆盖）#[tauri::command]async fn set_video_tags(video_id: i64, tag_ids: Vec<i64>) -> Result<(), String>;// 批量打标签#[tauri::command]async fn batch_add_tags(video_ids: Vec<i64>, tag_ids: Vec<i64>) -> Result<(), String>;
```

### 5.4 日志与统计模块

```rust
// 记录打开事件#[tauri::command]async fn log_video_open(video_id: i64) -> Result<i64, String>; // 返回 log_id// 记录关闭事件#[tauri::command]async fn log_video_close(log_id: i64) -> Result<(), String>;// 获取仪表盘统计数据#[tauri::command]async fn get_dashboard_stats() -> Result<DashboardStats, String>;// 获取观看趋势#[tauri::command]async fn get_view_trend(range: String) -> Result<Vec<TrendPoint>, String>; // range: "day" | "week" | "month"// 获取排行榜#[tauri::command]async fn get_leaderboard(category: String, limit: u32) -> Result<Vec<LeaderboardItem>, String>;// 导出日志#[tauri::command]async fn export_logs(filter: LogFilter, output_path: String) -> Result<(), String>;
```

### 5.5 设置模块

```rust
#[tauri::command]async fn get_settings() -> Result<HashMap<String, String>, String>;#[tauri::command]async fn set_setting(key: String, value: String) -> Result<(), String>;
```

## 六、前端架构设计

### 6.1 目录结构

```rust
src/├── assets/             # 静态资源├── components/         # 通用组件│   ├── Layout/         # 布局组件│   ├── VideoCard/      # 视频卡片│   └── TagSelect/      # 标签选择器├── pages/              # 页面│   ├── Dashboard/      # 仪表盘│   ├── Library/        # 视频库（树+列表）│   ├── Statistics/     # 统计分析│   ├── Tags/           # 标签管理│   └── Settings/       # 设置├── stores/             # Zustand 状态├── services/           # Tauri 命令封装├── types/              # TypeScript 类型├── hooks/              # 自定义 Hooks├── utils/              # 工具函数└── i18n/               # 国际化
```

### 6.2 状态管理（Zustand）

```rust
// stores/useLibraryStore.tsinterface LibraryState {  selectedFolderId: number | null;  viewMode: 'list' | 'grid' | 'detail';  searchKeyword: string;  filters: { tags: number[]; codecs: string[] };  setSelectedFolder: (id: number | null) => void;  setViewMode: (mode: 'list' | 'grid' | 'detail') => void;  setSearch: (keyword: string) => void;}// stores/useScanStore.tsinterface ScanState {  isScanning: boolean;  progress: number;      // 0-100  currentPath: string;  startScan: (path: string) => Promise<void>;  cancelScan: () => Promise<void>;}// stores/useTagStore.tsinterface TagState {  tagGroups: TagGroupWithTags[];  loadTags: () => Promise<void>;  createTag: (tag: TagInput) => Promise<void>;}
```

### 6.3 核心类型定义

```rust
// types/video.tsexport interface VideoInfo {  id: number;  fileName: string;  filePath: string;  fileSize: number;  duration: number;  width: number;  height: number;  codec: string;  fps: number;  coverPath: string | null;  openCount: number;  tags: Tag[];  modifiedAt: string;}export interface VideoQuery {  folderId?: number;  keyword?: string;  tagIds?: number[];  page: number;  pageSize: number;  sortBy: 'name' | 'size' | 'duration' | 'openCount' | 'modifiedAt';  sortOrder: 'asc' | 'desc';}
```

### 6.4 API 调用层

```typescript // services/api.ts import { invoke } from ‘@tauri-apps/api/core’;
export const videoApi = { list: (query: VideoQuery) => invoke<PageResult>(‘list_videos’, { query }),
